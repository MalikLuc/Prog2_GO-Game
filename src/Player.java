// player in go game
public class Player {
    // the player's color
    private int color;
    // the player's constructor
    public Player(int color) {
        this.color = color;
    }
    // returns the player's color
    public int getColor() {
        return color;
    }
}
